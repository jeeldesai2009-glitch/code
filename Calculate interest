#include <stdio.h>

int main() {
    float p, r, n, interest;

    printf("Enter Principal, Rate and Time: ");
    scanf("%f %f %f", &p, &r, &n);

    interest = (p * r * n) / 100;
    printf("Interest = %.2f", interest);

    return 0;
}